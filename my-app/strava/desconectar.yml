info:
  name: desconectar
  type: http
  seq: 3

http:
  method: DELETE
  url: '{{baseUrl}}/integrations/strava'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
