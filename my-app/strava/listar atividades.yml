info:
  name: listar atividades
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/integrations/strava/activities?inscriptionId={{inscriptionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
