info:
  name: status
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/integrations/strava/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5