info:
  name: pegando as tarefas de um desafio
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/tasks/get-tasks/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
