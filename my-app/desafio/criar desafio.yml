info:
  name: criar desafio
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/desafio/create"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
