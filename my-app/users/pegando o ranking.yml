info:
  name: pegando o ranking
  type: http
  seq: 4

http:
  method: GET
  url: /users/get-ranking/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
