info:
  name: pegando dados do usuario
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/users/get-user-data"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
