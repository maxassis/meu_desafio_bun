info:
  name: deletar avatar
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/users/upload-avatar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
