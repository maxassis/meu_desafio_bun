info:
  name: verificar o codigo
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/auth/email-otp/verify-email
  headers:
    - name: Origin
      value: http://localhost:3000
  body:
    type: json
    data: |-
      {
        "email": "teste@example.com",
        "otp": "35315"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
