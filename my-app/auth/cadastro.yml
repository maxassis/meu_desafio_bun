info:
  name: cadastro
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-up/email"
  headers:
    - name: Origin
      value: http://localhost:5173
  body:
    type: json
    data: |-
      {
        "name": "Bondis",
        "email": "fernandaassis.ux@gmail.com",
        "password": "12345678",
        "callbackURL": "{{baseUrl}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
