info:
  name: cadastro
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-up/email"
  headers:
    - name: Origin
      value: http://127.0.0.1:5500
  body:
    type: json
    data: |-
      {
        "name": "max",
        "email": "max.assis@outlook.com",
        "password": "12345678",
        "callbackURL": "{{baseUrl}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
