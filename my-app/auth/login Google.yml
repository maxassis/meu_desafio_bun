info:
  name: login Google
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/auth/sign-in/social
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
