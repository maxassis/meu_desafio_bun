info:
  name: session token
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/auth/get-session"
  headers:
    - name: Origin
      value: "{{baseUrl}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
