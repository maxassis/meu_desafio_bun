info:
  name: requisitando codigo para password reset
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/auth/email-otp/request-password-reset"
  headers:
    - name: Origin
      value: http://127.0.0.1:5500
  body:
    type: json
    data: |-
      {
        "email": "max.assis@ymai.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
