info:
  name: verificar codigo no reset de senha
  type: http
  seq: 7

http:
  method: POST
  url: '{{baseUrl}}/api/auth/email-otp/check-verification-otp'
  headers:
    - name: Origin
      value: http://127.0.0.1:5500
  body:
    type: json
    data: |-
      {
        "email": "teste@example.com",
        "type": "forget-password",
        "otp": "52445"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
